import React from 'react';
import IconsWithMetrics from './IconsWithMetrics';

const IconMetrics = () => {
    return (
        <div className="container">
            <IconsWithMetrics /> {/* includes two rows */}
        </div>
    );
}

export default IconMetrics;